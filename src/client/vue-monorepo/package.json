{
    "name": "vue-monorepo",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "dev": "pnpm -r --filter './packages/*' run dev",
        "build": "pnpm -r run build",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "typescript": "^5.6.3"
    },
    "workspaces": [
        "packages/*",
        "apps/*"
    ]
}
