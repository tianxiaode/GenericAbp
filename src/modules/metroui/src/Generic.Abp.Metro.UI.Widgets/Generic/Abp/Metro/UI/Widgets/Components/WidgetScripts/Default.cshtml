@model Generic.Abp.Metro.UI.Widgets.Components.WidgetResourcesViewModel
@if (Model.Widgets.Any())
{
    var resourceItems = Model.Widgets.SelectMany(w => w.Scripts).ToArray();
    if (resourceItems.Any())
    {
        <metro-script-bundle>
            @foreach (var resourceItem in resourceItems)
            {
                <metro-script src="@resourceItem.Src" />
            }
        </metro-script-bundle>
    }
}