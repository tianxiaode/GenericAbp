using System.Linq;
using Microsoft.EntityFrameworkCore;

namespace Generic.Abp.ExportManager.EntityFrameworkCore
{
    public static class ExportManagerQueryableExtensions
    {
        /*
        
        public static IQueryable<TEntity> IncludeDetails(this IQueryable<TEntity> queryable, bool include = true)
        {
            if (!include)
            {
                return queryable;
            }

            return queryable
                .Include(x => x.Users);
        }

        */

    }
}